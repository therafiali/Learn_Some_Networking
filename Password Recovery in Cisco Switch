# 1 Disconnect the power cable
# 2 hold mode button and connect power
# 3 Keep holding the button(s) until you see the power recovery menu (or similar screen) appear.
# 4 then type `flash_init`
# 5 then type `rename flash:config.text flash:config.text.old`
# 6 then type `boot`
# 7 then type `yes`
# 8 then type `no`

